# Outputs

`packages/` contains the generated output ZIP produced by notebook 02.

`tables/`, `figures/`, and `source_data/` contain extracted outputs for inspection.

`auxiliary/` contains additional generated support files that are useful for audit but are not presented as main manuscript tables.
