# Environment

The intended execution route is Google Colab. The notebooks install missing Python packages at runtime where needed.

The plain-Python files in `scripts/` mirror the notebooks and can be inspected or adapted for local use. Local execution was not the primary workflow for the submitted analysis.

Main Python packages are listed in `requirements.txt`.
